package com.lexi.flashcards.data.db

/** How a card is categorized in the UI (filters, badges) - purely cosmetic, doesn't affect scheduling. */
enum class CardType {
    WORD,
    PHRASAL_VERB,
    IDIOM,
    EXPRESSION,
}
